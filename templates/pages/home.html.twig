{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <img src="https://via.placeholder.com/2000" alt="" class="imageFond">
    </div>
    <div class="container">
        <div class="row">
            {% for trick in tricks %}
                <div class="col-md-3">
                    {% include('tricks/_trick.html.twig') %}
                </div>
            {% endfor %}
        </div>
    </div>
    

{% endblock %}
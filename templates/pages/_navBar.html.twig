<div class="navbar-high-display  mb-5">
    <nav class="navbar navbar-expand-lg navbar-light">
        <a class="navbar-brand" href="{{ path('home') }}">Snowtricks</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item mr-2">
                    <a class="nav-link" href="{{ path('home') }}">Home</span></a>
                </li>
                {% if not app.user %}
                    <li class="nav-item mr-2">
                        <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                    <li class="nav-item mr-2">
                        <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                    </li>
                {% else %}
                    {% if app.user %}
                        <li class="nav-item mr-2">
                            <a class="nav-link" href="{{ path('admin.trick.create') }}">Ajouter un trick</a>
                        </li>
                    {% endif %}
                    <li class="nav-item mr-2">
                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                    {% if app.user %}
                        <li class="nav-item mr-2">
                            <a class="nav-link" href="{{ path('user') }}"><i class="fas fa-user fa-2x"></i></a>
                        </li>
                    {% endif %}
                {% endif %}
            </ul>
        </div>
    </nav>
</div>